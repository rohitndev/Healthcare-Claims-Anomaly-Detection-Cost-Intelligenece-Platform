# ---- Core (required; the pipeline runs end-to-end with only these) ----
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyarrow>=12.0
matplotlib>=3.7

# ---- Testing ----
pytest>=7.4

# ---- Optional: distributed processing (PySpark / Databricks) ----
# pyspark>=3.5

# ---- Optional: HIPAA PHI anonymization (Microsoft Presidio) ----
# presidio-analyzer>=2.2
# presidio-anonymizer>=2.2
# spacy>=3.7
# (after install: python -m spacy download en_core_web_lg)

# ---- Optional: ClinicalBERT discharge-note analysis ----
# torch>=2.1
# transformers>=4.40

# ---- Optional: LLM audit narratives (Groq / Mixtral) ----
# groq>=0.11

# ---- Optional: AWS (S3 data lake + SNS alerts) ----
# boto3>=1.34

# ---- Optional: Snowflake warehouse ----
# snowflake-connector-python>=3.7

# ---- Optional: orchestration (Airflow / AWS MWAA) ----
# apache-airflow>=2.8

# ---- Optional: explainability ----
# shap>=0.44
